# Vercel-compatible requirements
# Core FastAPI dependencies
fastapi==0.104.1
uvicorn==0.24.0
pydantic[email]==2.5.0
pydantic-settings==2.1.0

# Authentication and Security (lightweight versions)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# HTTP client and async support
httpx==0.25.2
aiofiles==23.2.1

# Environment configuration
python-dotenv==1.0.0

# AI services (lightweight)
openai==1.3.7
groq==0.4.1

# Basic file processing
PyPDF2==3.0.1
python-docx==1.1.0

# Email service
resend==0.6.0

# Basic utilities
requests==2.31.0
geopy==2.4.0

# Note: Removed heavy dependencies incompatible with Vercel:
# - psycopg2-binary (database connections should use connection pooling)
# - redis, celery (use Vercel's serverless functions instead)
# - opencv-python, numpy, pillow (too large for serverless)
# - chromadb, sentence-transformers (too heavy)
# - pandas, openpyxl, scipy, scikit-learn (large ML libraries)
# - websockets (use Vercel's WebSocket support)
